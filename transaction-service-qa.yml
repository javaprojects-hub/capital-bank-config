server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/captialsbank_qa
    username: root
    password: c6e24f77874b1173345e1ee506d44aa3b05796d556b8c68d58db632bc3639618
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      pool-name: CapitalsTransactionPool
      maximum-pool-size: 50
      minimum-idle: 10
      idle-timeout: 60000          # 1 minute
      connection-timeout: 30000   # 30 seconds
      max-lifetime: 1800000        # 30 minutes
      leak-detection-threshold: 2000
      auto-commit: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  cloud:
    openfeign:
      client:
        config:
          capitals-bank:
            connectTimeout: 5000
            readTimeout: 5000
transaction:
  message: "welcome to the capitals bank transaction qa apis"
  contactDetails:
    name: "Ramesh - QAT"
    email: "ramesh@capitalsbank.com"
    onCallSupport:
      - "7684743900"
      - "7684743901"
